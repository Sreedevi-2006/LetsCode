class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x: int) -> int:
        sum = 0
        n = x
        while x!=0:
            d = x%10
            sum += d
            x = x//10
        if n%sum == 0:
            return sum
        else:
            return -1
