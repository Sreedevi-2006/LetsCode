#subtraction of product and sum
class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        sum = 0
        product = 1
        while n!=0:
            d = n%10
            n = n//10
            sum = sum + d
            product = product*d
        res = product - sum
        return res
            
